{
  "scripts": {
    "build": "rm -rf dist/ && npx tsc && node ./dist/src/app.js",
    "cleanBuild": "rm -rf dist/ && rm -rf node_modules/ && npm install && npx tsc && node ./dist/src/app.js"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^25.0.3",
    "jest": "^30.2.0",
    "typescript": "^5.9.3"
  }
}
